# Exercise 5 — Protect the feedback channel

Broad .NET examples pass, but the mutation-style check shows the registration contract lacks focused boundary cases. `make test` is green; `make mutation-test` is intentionally red. Strengthen tests without weakening feedback.
