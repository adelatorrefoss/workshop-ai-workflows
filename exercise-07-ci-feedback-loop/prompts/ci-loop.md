Execute GitHub Issue #<issue-number> using `AGENTS.md`. Implement and validate locally, create a semantic commit and pull request, then watch GitHub Actions. If CI fails, inspect its logs and repair the engineering cause, with at most three CI runs total. Never weaken feedback mechanisms. Stop before merge and report the PR URL, corrections, and exact local and CI results.

