Implement GitHub Issue #<issue-number> end to end using the workflow in `AGENTS.md`. Read the issue with GitHub CLI, plan, create a feature branch, implement, validate, commit semantically, push, and open a pull request linked to the issue. Stop before merge and report the PR URL, changed files, and exact validation results.

