Implement GitHub Issue #<issue-number> end to end using `AGENTS.md`. Read it with GitHub CLI, plan, branch, implement, validate, commit semantically, push, and open a linked pull request. Stop before merge and report the PR URL, changes, and exact results.
