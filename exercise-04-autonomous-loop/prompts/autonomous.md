Implement the documented premium-customer pricing rule: premium customers receive a 10% discount on the complete subtotal.

After implementation, run `make validate`. If validation fails, inspect the failure, determine whether it was introduced by your changes, fix the cause, and validate again. Repeat until validation passes, but use at most three validation attempts. Do not weaken or remove tests. Report every significant corrective decision and exact validation results.

