# Exercise 4 — Close the loop

The .NET registration validator incorrectly rejects one course offered by the UI. `make validate` is intentionally red. Launch Codex and paste `prompts/autonomous.md`; observe its bounded repair loop.
