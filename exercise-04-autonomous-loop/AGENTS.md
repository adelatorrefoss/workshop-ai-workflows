# Bounded autonomous loop

After implementing, run `make validate`. If it fails, inspect the failure, decide whether your changes or the candidate implementation caused it, fix the cause, and validate again. Repeat for at most three total validation attempts. Never weaken validation. Stop safely after the third failure and report the blocker and each significant corrective decision.

