# Exercise 3 — Introduce executable feedback

This .NET registration endpoint saves successfully but violates the required dependency direction. `make build` is green and `make architecture-test` is intentionally red. Launch Codex and paste `prompts/implement.md`.
