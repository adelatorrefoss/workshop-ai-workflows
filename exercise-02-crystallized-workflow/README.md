# Exercise 2 — Crystallize the workflow

The working method now lives in `AGENTS.md`; task prompts contain outcomes rather than repeated process instructions.

## Starting state

`make validate` passes. Run each feature on a separate clean branch.

## Feature A

Launch Codex and paste `prompts/feature-a.md`.

## Feature B

Return to the original starting state on another branch and paste `prompts/feature-b.md`.

After both runs, identify which instructions belong to the task and which belong to the engineering environment.

