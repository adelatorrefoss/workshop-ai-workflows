# Exercise 2 — Crystallize the workflow

The same .NET 10 registration application now tells Codex how features should be developed through `AGENTS.md`. Run each prompt from the green starter state on a separate branch.

- Feature A adds an operational endpoint.
- Feature B adds registration validation across the API contract.

After both, separate instructions belonging to the task from reusable instructions belonging to the engineering environment.
