Validate interest registrations. Name and Course are required; Email must be syntactically valid. Invalid requests return HTTP 400 and are not written to `interests.txt`. Preserve valid behavior and add automated validation.
